<h1>Users</h1>
<table class="table">
  <%= for user <- @users do %>
    <tr>
      <td><%= user.email %></td>
        (<%= link "View", to: user_path(@conn, :show, user) %>)
    </tr>
  <%= end %>
<table>
